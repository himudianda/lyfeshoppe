{% import 'dashboard/macros/form.jinja2' as f with context %}

{% if request.endpoint.endswith('new') %}
  {% set endpoint = 'backend.business_reservations_new' %}
  {% set form_kwargs = {'id': business.id} %}
  {% set legend = 'Add a new Reservation' %}
  {% set button = _('Add') %}
{% else %}
  {% set endpoint = 'backend.business_product_edit' %}
  {% set form_kwargs = {'id': business.id, 'product_id': reservation.id} %}
  {% set legend = 'Update this reservation' %}
  {% set button = _('Save') %}
{% endif %}

<div class="row">
    <div class="col-md-4 col-md-offset-4 well">
      {% call f.form_tag(endpoint, **form_kwargs) %}

        {% call f.form_group(form.status, css_class='sm-margin-bottom') %}
        {% endcall %}

        <div class="dt relative">
          {% call f.form_group(form.start_time) %}
          {% endcall %}
        </div>

        <div class="dt relative">
          {% call f.form_group(form.end_time) %}
          {% endcall %}
        </div>

          <hr>
          <div class="row">
              <div class="col-md-6">
                  <button type="submit" class="btn btn-primary btn-block">
                      <img src="{{ asset_url_for('images/spinner.gif') }}"
                           class="spinner"
                           width="16" height="11" alt="Spinner"/>
                    {{ button }}
                  </button>
              </div>
              <div class="col-md-6">
                  <div class="visible-xs visible-sm sm-margin-top"></div>
                  <a href="{{ url_for('backend.business_reservations', **form_kwargs) }}"
                     class="btn btn-default btn-block">
                    {{ _('Cancel') }}
                  </a>
              </div>
          </div>
      {% endcall %}
    </div>
</div>
